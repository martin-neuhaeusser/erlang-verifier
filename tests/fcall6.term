debug reduce in SEM_EQUATIONS : 
(sys-start, < tau | #no-res
       | let var("Fun") = fun() -> int(2) in
	     letrec (atom("test") / int(0) = fun() -> apply var("Fun") ()) in
	         apply atom("test") / int(0) ()
       | pid(0) 
       | #empty-mbox 
       | #empty-pid-seq 
       | false 
       | #empty-modenv >,
#empty-modenv,
pid(0))

.
