set trace substitution off .
debug reduce in THEORY : 
(sys-start, < tau | #no-res
       | do 
               case {int(0)} of
	            {var("Var")} when atom("true") -> var("Var")
	       end
	       case {int(1)} of
	            {var("Var")} when atom("true") -> var("Var")
	       end
	| pid(0) 
	| #empty-mbox 
	| #empty-pid-seq 
	| false 
	| #empty-modenv >, 
#empty-modenv, pid(0)) .
