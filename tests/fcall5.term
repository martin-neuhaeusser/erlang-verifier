debug reduce in SEM_EQUATIONS : 
(sys-start, < tau | #no-res
       | letrec (atom("endless") / int(0) = fun() -> apply atom("endless") / int(0) ()) in
		 apply atom("endless") / int(0) ()
       | pid(0) 
       | #empty-mbox 
       | #empty-pid-seq 
       | false 
       | #empty-modenv >,
#empty-modenv,
pid(0))

.
